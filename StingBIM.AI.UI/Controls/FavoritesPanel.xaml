<UserControl x:Class="StingBIM.AI.UI.Controls.FavoritesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">

    <UserControl.Resources>
        <!-- Favorite Item Style -->
        <Style x:Key="FavoriteItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ItemActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Opacity" Value="0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Path Grid.Column="0"
                      Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z"
                      Fill="{DynamicResource WarningBrush}"
                      Width="18" Height="18" Stretch="Uniform"
                      Margin="0,0,8,0" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           Text="Favorites"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource ForegroundBrush}"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="2"
                        x:Name="CloseButton"
                        Click="CloseButton_Click"
                        Style="{StaticResource ItemActionButtonStyle}"
                        ToolTip="Close">
                    <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                          Fill="{DynamicResource ForegroundSecondaryBrush}"
                          Width="12" Height="12" Stretch="Uniform"/>
                </Button>
            </Grid>

            <!-- Add New Favorite -->
            <Border Grid.Row="1"
                    Background="{DynamicResource CardBrush}"
                    CornerRadius="6"
                    Padding="8"
                    Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="NewFavoriteTextBox"
                             Grid.Column="0"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource ForegroundBrush}"
                             FontSize="12"
                             VerticalContentAlignment="Center"
                             KeyDown="NewFavoriteTextBox_KeyDown"/>

                    <TextBlock Grid.Column="0"
                               x:Name="PlaceholderText"
                               Text="Type a command to save..."
                               Foreground="{DynamicResource PlaceholderBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               IsHitTestVisible="False"
                               Margin="2,0,0,0"/>

                    <Button Grid.Column="1"
                            x:Name="AddButton"
                            Click="AddButton_Click"
                            Style="{StaticResource ItemActionButtonStyle}"
                            ToolTip="Add to favorites"
                            Opacity="0.8">
                        <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                              Fill="{DynamicResource AccentBrush}"
                              Width="16" Height="16" Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Favorites List -->
            <ListBox Grid.Row="2"
                     x:Name="FavoritesList"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource FavoriteItemStyle}"
                     SelectionMode="Single"
                     MouseDoubleClick="FavoritesList_MouseDoubleClick"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Star Icon -->
                            <Path Grid.Column="0"
                                  Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z"
                                  Fill="{DynamicResource WarningBrush}"
                                  Width="12" Height="12" Stretch="Uniform"
                                  Margin="0,0,8,0" VerticalAlignment="Center"
                                  Opacity="0.7"/>

                            <!-- Command Text -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding DisplayName}"
                                           FontSize="12"
                                           FontWeight="Medium"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Command}"
                                           FontSize="10"
                                           Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding ShowCommand, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>

                            <!-- Actions -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Style="{StaticResource ItemActionButtonStyle}"
                                        Click="EditButton_Click"
                                        Tag="{Binding}"
                                        ToolTip="Edit">
                                    <Path Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z"
                                          Fill="{DynamicResource ForegroundSecondaryBrush}"
                                          Width="12" Height="12" Stretch="Uniform"/>
                                </Button>
                                <Button Style="{StaticResource ItemActionButtonStyle}"
                                        Click="DeleteButton_Click"
                                        Tag="{Binding}"
                                        ToolTip="Remove">
                                    <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
                                          Fill="{DynamicResource ForegroundSecondaryBrush}"
                                          Width="12" Height="12" Stretch="Uniform"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Empty State -->
            <StackPanel Grid.Row="2"
                        x:Name="EmptyState"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Visibility="Collapsed">
                <Path Data="M12,17.27L18.18,21L16.54,13.97L22,9.24L14.81,8.62L12,2L9.19,8.62L2,9.24L7.45,13.97L5.82,21L12,17.27Z"
                      Fill="{DynamicResource ForegroundSecondaryBrush}"
                      Width="32" Height="32" Stretch="Uniform"
                      HorizontalAlignment="Center"
                      Opacity="0.3"/>
                <TextBlock Text="No favorites yet"
                           FontSize="12"
                           Foreground="{DynamicResource ForegroundSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,4"/>
                <TextBlock Text="Add commands you use frequently"
                           FontSize="11"
                           Foreground="{DynamicResource ForegroundSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Opacity="0.7"/>
            </StackPanel>

            <!-- Keyboard Hint -->
            <TextBlock Grid.Row="3"
                       Text="Double-click to use • Enter to add"
                       FontSize="10"
                       Foreground="{DynamicResource ForegroundSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"
                       Opacity="0.7"/>
        </Grid>
    </Border>
</UserControl>
